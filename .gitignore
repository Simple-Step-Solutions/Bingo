# Unanchored: functions/ has its own package.json and its own node_modules.
node_modules/
/dist/
/coverage/
/.DS_Store
/*.log
.env*
!.env.example
# Deploy-time function params. Not secret: the database id already ships
# to every browser in the client bundle.
!functions/.env.sss-hvgcc-bingo
/firebase-applet-config.json
/.claude/
