# Deploy-time parameter values for this project.
#
# Read by firebase-tools when it resolves defineString() params. The database id
# is NOT a secret -- it already ships to every browser as
# VITE_FIREBASE_DATABASE_ID -- so it lives in the repo rather than in a CI
# secret, where an empty value would silently point every Cloud Function at an
# empty (default) database.
FIRESTORE_DATABASE_ID=ai-studio-b22a3d46-2072-4ec8-b7cc-b2370d5fdd10
